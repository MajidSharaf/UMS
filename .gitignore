__pycache__/
*.pyc
.venv/
venv/
.env

# Generated at runtime, not source
experiments/lab.db
experiments/runs/*.json
!experiments/runs/.gitkeep

.streamlit/secrets.toml
.DS_Store

# Machine-specific: which prompt/model is active per stage depends on which
# models you've actually pulled. Set via the Pipeline Settings page.
pipeline_config.yaml
